<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>냐냐냥</title>
    <link rel="stylesheet" href="index.css">
</head>
<body>
<div style="width:100%; height:55px; background:#bacaca; display:flex; align-items:center">
    <div style="position:relative; left:2%"><h2>Your Began Map</h2></div>
    <% if(is_logined == false){ %>
        <div id="username" style="display:none;">null</div>
        <div onclick="loginPage()" style="position:absolute; right:450px;">🌿 Signin</div>
        <div onclick="signupPage()" style="position:absolute; right:340px;">🍀 Signup</div> 
    <% }else{ %>
        <div id="username" style="position:absolute; left:250px;"><%= username %></div>
        <div style="position:absolute; left:290px;"> 님 환영합니다!</div>
        <div onclick="logoutPage()"style="position:absolute; right:340px;">🌿 Logout</div>
    <% } %>
    <div id="searchTgl" onclick="searchTgl()" style="position:absolute; right:235px;">🍃 Search</div>
    <div id="ShareTgl" onclick="shareSNS()" style="position:absolute; right:140px;">🌲 Board</div>
    <div id="ContactTgl" style="position:absolute; right:35px;">🌱 Contact</div>
</div>

<div id="map" style="width:100%; height:600px; position:relative; overflow:hidden;"></div>

<!-- 지도타입 컨트롤 div / 이 식당은 하늘이에요.. 뭐 이런-->
<% if(is_logined == false){ %>
<% }else{ %>
    <div class="custom_typecontrol radius_border">
        <span id="btnRoadmap" class="selected_btn" onclick="setMapType('roadmap')">지도</span>
        <span id="btnSkyview" class="btn" onclick="setMapType('skyview')">내꺼</span>
    </div>
<% } %>

<script type="text/javascript" src="//dapi.kakao.com/v2/maps/sdk.js?appkey=edcd3f117657acc449c5e16721364cd3&libraries=services"></script>
<script type="text/javascript" src="http://code.jquery.com/jquery-3.5.1.min.js"></script>
<script src="index.js"></script>
</body>
</html>